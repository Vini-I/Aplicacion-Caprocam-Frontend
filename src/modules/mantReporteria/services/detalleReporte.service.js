export async function obtenerDetalleRegistro({
  tipoRegistro,
  fincaId,
  estanqueId,
}) {

  /*
     Aquí irá después:

     GET /api/registros/detalle

     Params:

     tipoRegistro
     fincaId
     estanqueId

 */

  return [];
}